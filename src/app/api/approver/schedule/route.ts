import { hydrateServerlessState } from '../../../../server/stateLoader'; import { listApproverSchedule } from '../../../../services/review-meetings/reviewMeetingsRouter'; export const dynamic = 'force-dynamic'; export const runtime = 'nodejs'; export async function GET(request: Request) { await hydrateServerlessState(); const result = listApproverSchedule(request.headers.get('x-user-id')); return Response.json(result.body, { status: result.status }); }
