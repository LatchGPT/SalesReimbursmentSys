import { hydrateServerlessState } from '../../../../server/stateLoader'; import { getActivityStatus } from '../../../../services/activity/userActivity'; export const dynamic = 'force-dynamic'; export const runtime = 'nodejs'; export async function GET(request: Request) { await hydrateServerlessState(); const result = getActivityStatus(request.headers.get('x-user-id')); return Response.json(result.body, { status: result.status }); }
