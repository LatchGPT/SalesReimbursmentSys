import { withPersistenceScope } from '../../../../lib/db/persistenceScope'; import { importCompanies } from '../../../../services/admin/companiesRouter'; import { hydrateServerlessState } from '../../../../server/stateLoader'; export const dynamic = 'force-dynamic'; export const runtime = 'nodejs';
export async function POST(request: Request) { return withPersistenceScope(async () => { await hydrateServerlessState(); const result = await importCompanies(request.headers.get('x-user-id'), await request.json()); return Response.json(result.body, { status: result.status }); }); }
