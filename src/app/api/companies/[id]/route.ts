import { withPersistenceScope } from '../../../../lib/db/persistenceScope'; import { updateCompany } from '../../../../services/admin/companiesRouter'; import { hydrateServerlessState } from '../../../../server/stateLoader'; export const dynamic = 'force-dynamic'; export const runtime = 'nodejs'; type Context = { params: Promise<{ id: string }> };
export async function PUT(request: Request, context: Context) { return withPersistenceScope(async () => { await hydrateServerlessState(); const { id } = await context.params; const result = await updateCompany(request.headers.get('x-user-id'), id, await request.json()); return Response.json(result.body, { status: result.status }); }); }
